{
  "name": "jif",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jrpat/jif"
  },
  "scripts": {
    "clean": "rm -rf dist .tmp",
    "test": "bun test --only-failures",
    "typecheck": "bunx tsc --noEmit",
    "build": "bun run scripts/build.ts",
    "start": "bun run index.ts",
    "sample": "bun run index.ts --sample",
    "dev": "bun run scripts/dev.ts",
    "bin": "bun run scripts/install-binary.ts",
    "release:preflight": "bun run scripts/release/preflight.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.4"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@opentui/core": "0.4.2",
    "@opentui/solid": "0.4.2",
    "fzy.js": "^0.4.1",
    "solid-js": "1.9.12"
  }
}
