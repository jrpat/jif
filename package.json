{
  "name": "jif",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist .tmp",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "build": "bun run scripts/build.ts",
    "start": "bun run index.ts",
    "sample": "bun run index.ts --sample",
    "dev": "bun --watch ./index.ts --sample",
    "install": "bun run scripts/install.ts",
    "live": "bun run install && jif"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@opentui/core": "^0.1.90",
    "@opentui/solid": "^0.1.90",
    "fzy.js": "^0.4.1",
    "solid-js": "^1.9.11"
  }
}
